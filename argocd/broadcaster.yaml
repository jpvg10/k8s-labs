apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: broadcaster
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jpvg10/k8s-labs
    path: broadcaster/kustomize/overlays/azure/
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: project
  syncPolicy:
    automated:
      prune: true
    syncOptions:
    - CreateNamespace=true
