apiVersion: v1
kind: Service
metadata:
  name: todo-app-svc
  namespace: project
spec:
  type: NodePort
  selector:
    app: todo-app
  ports:
    - port: 1234
      protocol: TCP
      targetPort: 5000
