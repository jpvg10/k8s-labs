apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: broadcaster-pv-claim
  namespace: project
spec:
  storageClassName: broadcaster-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
