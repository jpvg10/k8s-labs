apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: broadcaster-pv-claim
  namespace: project
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
